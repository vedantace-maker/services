// constants/api.ts  ← create this file
const DEV_IP = '192.168.1.5';      // ← replace with YOUR machine's IPv4
const PORT   = '3000';             // ← replace with your server port

export const API_BASE_URL = `http://${DEV_IP}:${PORT}`;
