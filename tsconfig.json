{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}


// import { useLocationStore } from '../store/locationStore';

// const resetLocation = useLocationStore((s) => s.reset);

// const handleLogout = () => {
//     useAuthStore.getState().logout();
//     resetLocation();  // ✅ clears all location data
// };